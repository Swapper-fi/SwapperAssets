const ethTokensList = require('../ETH/tokenlist.json')
const bscTokensList = require('../BSC/tokenlist.json')

const startScripts = () => {
  const ethTokens = ethTokensList.tokens
  const bscTokens = bscTokensList.tokens
}

startScripts()
